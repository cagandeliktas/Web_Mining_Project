name: "dbt_project"
version: "1.0.0"
config-version: 2

profile: "dbt_project"

model-paths: ["models"]

models:
  dbt_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
